# Studio Nivedita Trace — V1

Mobile web app for tracing a reference onto a physical canvas.

Features: camera background, reference upload, opacity, scale, rotation, drag, pinch, lock, grid, flip, reset and PWA support.

Camera access requires HTTPS when deployed.